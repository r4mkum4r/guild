/**
 * `notify`
 *
 * ---------------------------------------------------------------
 *
 * Desktop notifications for grunt task errors.
 *
 * For usage docs see:
 *   https://www.npmjs.com/package/grunt-notify
 *
 */
module.exports = function(grunt) {

  grunt.loadNpmTasks('grunt-notify');
};
